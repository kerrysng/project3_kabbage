<!DOCTYPE html>
<html>
  <head>
    <title>KabbageApp</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
  </head>

  <body>

    <ul class="topnav">
      <% if user_signed_in? %>
       <a href="/dashboard">Dashboard</a>
       <a href="/">Logout</a>
        <% if session[:first_name] != nil %>
         <p>Logged in: <%= session[:first_name] %><%=session[:id]%></p>
         <%= form_tag '/', method: 'delete' do %>
        <%end%>
        <%end%>

      <% else %>

      <li class ="dropdown"><a href="/" class="dropdown-toggle" data-toggle="dropdown">Login</a>
        <ul class="dropdown-menu" >
          <%= form_tag '/', method: 'post' do %>
            <label for="">email</label>
              <input type="text" name="email">
                <label for="">password</label>
                  <input type="password" name="password">
                    <!-- <button>login</button> -->
                     <% end %>
        </ul>
                 <li><a href="/user">Sign Up
               <!-- <%= form_tag '/user/new', method: 'get' do %> -->
            <% end %>
          </li>

      <%end%>

    </ul>

    <% if session[:first_name] != nil %>
      <!-- <p>Logged in: <%= session[:first_name] %><%=session[:id]%></p>
      <%= form_tag '/', method: 'delete' do %>
      <button>logout</button>
      <% end %> -->
      <%= form_tag '/new_card_form', method: 'get' do %>
        <button>Create Card</button>
      <% end %>
    <% end %>
    <%= yield %>
  </body>
</html>
